{
  "name": "github.io-api",
  "version": "1.0.0",
  "description": "deploying a simple express app to vercel",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "5.0.1",
    "fs": "^0.0.2-security"
  }
}